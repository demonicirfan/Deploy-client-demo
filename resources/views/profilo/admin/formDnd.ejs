<% if(user.role == 'admin') { %>

<section class="section-two">
    <h1> Inserisci il nuovo post di D&D</h1>
    <form action="/profiloAdmin/postDnD" method="POST" class="bg-white rounded px-8 pt-6 pb-8 mb-4">
        <% if(messages.error) { %>
        <span class="text-red-500 text-sm"><%= messages.error %></span>
        <% } %>
        <div class="mb-4">
            <label for="name" value="<%= messages.name %>" class="block text-gray-700 text-sm font-bol mb-2">Titolo</label>
            <input name="name" type="text" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" id="name" placeholder="Titolo">
        </div>
        <div class="mb-4">
            <label for="descrizione" class="block text-gray-700 text-sm font-bol mb-2">Descrizione</label>
            <input name="descrizione" value="<%= messages.usurname %>" type="text" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" id="descrizione" placeholder="Descrizione">
        </div>
        <div class="mb-4">
            <label for="tags" class="block text-gray-700 text-sm font-bol mb-2">Tags</label>
            <input name="tags" value="<%= messages.email %>" type="text" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" id="tags" placeholder="#tags">
        </div>
        <div class="mb-6">
            <label for="link_telegram" class="block text-gray-700 text-sm font-bold mb-2">Link Post</label>
            <input name="link_telegram" type="text" id="link_telegram" placeholder="Link Telegram" class="shadow appearance-none borer rouned w-full py-2 px-3 text-gray-700 mb-3 leading-tight focus:outline-none focus:shadow-outline">
        </div>
        <div class="flex items-center justify-between">
            <button type="submit" class="font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Pubblica</button>
        </div>
    </form>
</section>
<% } else{ %>

    <section class="section-two">
        <h1>Non sei un amministratore!!!</h1>
        <p>Ciao avventuriero, ti sei perso. Questa area del sito è dedicata solo agli amministratori, mi dispiace. Torna alla <a href="/">home</a> el sito.</p>
    </section>

<% } %>