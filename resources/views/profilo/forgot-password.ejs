<section class="section-two">
    <h1>Recupero password</h1>
    <p>Benvenuto avventuriero nel portale per recuperare la tua chiave di accesso. Sei sicuro di avarla smarrita? Hai controllato nello zaino o nell'ultima taverna in cui sei stato?</p>

    <form action="/forgotpassword" method="post" class="bg-white rounded px-8 pt-6 pb-8 mb-4">
        <% if(messages.error) { %>
            <span class="text-red-500 text-sm"><%= messages.error %></span>
            <% } %>
        <div class="mb-4">
            <label for="email" class="block text-gray-700 text-sm font-bold mb-2">Email</label>
            <input type="email" name="email" id="email" class="shadow appearance-none borer rouned w-full py-2 px-3 text-gray-700 mb-3 leading-tight focus:outline-none focus:shadow-outline" placeholder="Email">    
        </div>
        <div class="flex items-center justify-between">
            <button type="submit" value="Invia" class="font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Invia</button>
        </div>
    </form>
    <p style="margin-top: 39px;">La prossima volta fai attenzione, ladri, barbari e mostri oscuri girovacano là fuori.</p>
</section>